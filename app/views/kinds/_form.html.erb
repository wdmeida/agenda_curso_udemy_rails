<%= form_for(kind, html: { class: "form-horizontal" }) do |f| %>
  <% if kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kind.errors.count, "error") %> prohibited this kind from being saved:</h2>

      <ul>
      <% kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :description, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :description, :class => "form-control" %>
    </div>
  </div>

  <div class="field">
    <%= f.submit t('buttons.save'), :class => "btn btn-primary" %>
    <%= link_to t('buttons.back'), kinds_path, :class => "btn btn-success" %>
  </div>
<% end %>
